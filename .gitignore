node_modules/
.dev.vars
.wrangler/
worker-configuration.d.ts

# `wrangler deploy --dry-run --outdir dist` — a measurement, not an artifact
dist/

# Claude Code worktrees + macOS noise
.claude/worktrees/
.DS_Store

# Signed binary minted from macos/share-to-cdn.plist — the plist is the source of truth
macos/*.shortcut

{
  // The quickshare test runs in Bun, like the hooks — same compiler options, and
  // `include` has to be re-declared because an inherited one resolves relative to
  // the config it came from. Editor ergonomics only; nothing typechecks in CI.
  "extends": "../hooks/tsconfig.json",
  "include": ["**/*.ts"]
}
